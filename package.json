{
  "name": "brainstem",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "bun-types": "^1.4.2",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
