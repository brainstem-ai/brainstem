# Task P0: Product contract docs, pinned versions, runtime contract tests

Repo: brainstem (bun + TS monorepo). packages/core = reflex engine, packages/cli = harness on @earendil-works/pi-agent-core 0.86.1.

Do exactly these four things, in order. No other changes.

## 1. README.md (repo root, new file)

Sections:
- One-liner: coding-agent harness where Jev (System One) makes bounded judgments (gate/sanitize/verify/pulse/steer) and the LLM only generates. Responsibility table: harness code owns execution/permissions/budgets/context; Jev owns narrow semantic judgments over supplied evidence; main model owns strategy/code/explanations; user owns intent/constraints/approvals.
- Reflexes: Gate, Sanitize, Verify, Pulse, Steer (implemented); Select, Focus (planned). Tend = later checkpoint workflow, not a continuous filter.
- Setup: `bun install`; `.env` needs TYPESAFE_API_KEY and ZAI_API_KEY (bun auto-loads). Example: `bun packages/cli/src/main.ts --cwd /tmp/repo --task "..." --trust 0.5`.
- Trust dial: trust raises/lowers the auto-run CONFIDENCE BAR (policy.ts: autoConfidence = 0.95 - 0.35*trust). trust 0 does NOT mean "ask about everything". Safety thresholds (deny lines, credential nouls) never change with trust.
- Approvals today: gate "ask" returns a blocked tool result telling the agent to ask the user. Full interactive lifecycle lands in P3.
- Replay: `bun packages/cli/src/main.ts replay <journal.ndjson> [--trust N]` re-scores recorded judgments offline. Limitations: static-floor denials have no recorded judgment; side effects are never replayed.
- Versions pinned deliberately (@earendil-works/pi-* 0.86.1); upgrades are explicit checks.

## 2. docs/plans/2026-09-20-brainstem.md

Add at the very top, above the title: one blockquote line: `> Superseded as current direction by docs/plans/2026-09-20-harness-evolution.md — historical results below are preserved.` Keep everything else unchanged.

## 3. packages/cli/package.json

Replace `"*"` versions with exact installed: "@earendil-works/pi-agent-core": "0.86.1", "@earendil-works/pi-ai": "0.86.1", and the exact installed "@sinclair/typebox" version (check node_modules/@sinclair/typebox/package.json). Run `bun install` after so the lockfile updates.

## 4. packages/cli/test/runtime-contract.test.ts (new)

A partial version of this file already exists on disk with known errors — fix/rewrite it to satisfy the contract below. Reuse helper patterns from packages/cli/test/harness.test.ts (mockSystemOne, scriptedStream, NO_USAGE, assistantMessage).

KNOWN DIAGNOSIS (verified against pi-agent-core 0.86.1 — do not re-derive):
- `TranscriptContext` is NOT exported from @earendil-works/pi-ai's root. Derive it instead: `type Ctx = Parameters<StreamFn>[1]`.
- `TranscriptContext` is `{ messages: Message[] }` (branded) — there is NO `tools` field. Tools reach the provider out-of-band, NOT via streamFn context. Do not assert `context.tools`.
- Three `assistantMessage([...])` calls (approx lines 185, 233, 272) are missing the required second `stopReason` argument — pass `"toolUse"`.
- A `contentIndex` access around line 330 needs a undefined-guard.

Write these scripted tests:
1. First-turn setup: after agent.prompt(), state.messages[0] is the system message containing the prompt; state.tools has the five tools.
2. Gate order: beforeToolCall block prevents execute (spy tool whose execute records calls; assert zero calls after blocked run).
3. After-tool replacement: afterToolCall content override is what lands in the transcript toolResult.
4. Gate "ask" behavior (current contract; add `// P3: refine when approval lifecycle lands`): blocked error tool result text starts with "[brainstem]" and instructs asking the user.
5. Turn-boundary tool changes — pin the EXECUTABLE contract: after run 1, `agent.state.tools = [spyTool]`; run 2 where the model calls spy (must execute and succeed) then calls the removed `bash` tool (must yield an error toolResult, not execute bash). Assert state.tools reflects the swap.
6. Prompt propagation: second agent.prompt() sends the full prior transcript (recordingStream captures context.messages roles per call).
7. Context projection probe: after a completed run, append a system message ("Skill: run make verify...") via setMessages and swap tools to [spy]; second request context must contain: intact tool_use/tool_result pairing (assert pair ids/names), the injected system message present, and state.tools === [spy]. Write the captured context JSON (messages roles + tool pairing + note that tools travel out-of-band) to packages/cli/test/fixtures/provider-payload.json (mkdirSync recursive, JSON.stringify 2-space). Add a comment: `// P8: verify provider-level payloads against a real adapter endpoint; context-level contract is pinned here.`

## Verify + commit

- `bun run test` (all pass, 61+ new tests) and `bun run typecheck` (0 errors).
- Commit: `P0: product contract docs, pinned versions, runtime contract tests`
