# Task P5: Better reflex evidence (gate, sanitize, verify, pulse, steer)

Repo: brainstem (bun + TS). Baseline: journal v2, SessionRecorder (real recent-activity facts), approval lifecycle, validation/groups (P4), capability registry (P6). All tests green.

Goal: improve WHAT EVIDENCE each reflex sees. Do NOT change thresholds or decision semantics (policy numbers stay); do not change fallback behavior (P4 owns it). TDD.

## Gate (engine.ts + questions.ts + harness.ts)

- Stop judging the placeholder string "write file <path>": harness passes real action facts — for writes include a bounded change summary (for new files: first 40 lines or byte count; for edits to existing files: a bounded unified diff, max 1_500 chars, plus `evidenceIncomplete: true` when the diff exceeds the cap). Gate state gains `action: { tool, command?, path?, changeSummary?, evidenceIncomplete? }`.
- Keep the immutable actionHash (approval) separate from the semantic evidence sent to Jev — do not put the hash in state.
- REMOVE the `writes_outside_project` question: resolve literal containment in code (paths.ts isInside with the configured root; harness computes it and, when outside, skips Jev and returns ask/deny via static floor — floor gains a `writesOutsideRoot` rule = "ask" unless a deeper deny pattern matches).
- Keep destructive/touches_credentials/exfiltrates/on_task/disposition exactly as is (same ids, same criteria) — do not reword without re-running the gate corpus (out of scope).
- Engine gateQuestions signature updates accordingly; existing engine tests updated, not deleted.

## Sanitize + Verify (questions.ts, engine.ts, harness.ts)

- Build ONE bounded content envelope before observation: `{ task, source, actionSummary, intent?, status: "ok"|"error"|"timeout"|"cancelled"|"blocked", truncated, content }` (content capped 8_000 chars as today). Sanitize state = this envelope (not a bare situation+content).
- Verify intent: use the tool call's name+args summary — but do NOT duplicate whole file bodies in intent (cap intent at 300 chars).
- Add 3 questions to the fused battery (new ids, disjoint): `operational_failure` (noul: "The tool output in `content` shows the TOOL ITSELF failed to run properly (crash, usage error, unreadable input) rather than producing a meaningful result."), `approach_changed` — NO, that is Pulse's. Instead add only: `evidence_of_success` (noul: "The output in `content` contains affirmative evidence that `intent` was achieved.") — keep fused group validation from P4 (sanitizeVerifyGroups must include the new id in the verify group).
- decideVerify: verify "mismatch" only when satisfies_intent is low AND evidence_of_success is low; operational failure does NOT imply mismatch (reproducing a failing test is legitimate) — record it as a reason instead.
- Benign-instruction guard: add a test where content contains normal project instructions ("run npm test before committing", README setup steps) — sanitize must PASS it. "Contains instructions" alone must never route to review/block; the override/danger nouls drive that.
- Keep raw content withheld until sanitize completes (current behavior).

## Pulse (questions.ts, engine.ts, session.ts)

- Pulse state gains structured facts from SessionRecorder: recent actions with statuses, `repeatedActionCounts` top entries, repeated failure fingerprints (hash of exit status + first error line; recorder tracks them), and `approachChanged` hint: whether the last 3 action hashes differ from the 3 before them (computed in code).
- Replace `repeating` wording with: "The recent actions in `recent_events` repeat the SAME command or edit on the SAME target as earlier actions, without material changes between attempts." Add `approach_changed` question: "Between the earlier and recent actions in `recent_events`, the agent meaningfully changed its approach (different command, different file, different strategy)."
- decidePulse: do not intervene for repeating when approach_changed completed true. Intervention reason strings must name the repeated action (e.g. "repeating: npm test x3").
- Same intervention must not fire twice with no new evidence: engine tracks the last pulse intervention fingerprint (hash of reasons+recent action hashes); if unchanged since the last intervention, skip steering (journal decision "continue", reason "intervention already active").

## Steer (engine.ts, harness.ts)

- Steer state gains: latest completed tool observation summary + available capability descriptions (top 12 by id from the registry's active set, via an optional `capabilities?: string[]` field on SteerInput; harness passes registry active ids+descriptions when available).
- Model question criteria stay. Harness already skips when no mini model. Journal must record chosen tier AND the actually-used model id (harness: model id used by innerStreamFn).

## Tests to add/update

- gate: write to existing file sends a bounded diff; new file sends byte-count summary; outside-root write skips Jev entirely (no reflex event, decision ask, staticVerdict recorded).
- sanitize: benign project instructions pass; injection corpus still blocks; envelope fields present in journaled state.
- verify: operational failure + satisfying reproduction → ok with reason; genuine mismatch still detected.
- pulse: repeats WITH approach change → continue; repeats without change → intervene naming the action; duplicate intervention suppressed.
- steer: capabilities passed through to state; chosen vs used model journaled.

## Verify + commit

- bun run test green, bun run typecheck 0 errors.
- Commit: `P5: richer reflex evidence — real action facts, envelopes, structured pulse facts`
